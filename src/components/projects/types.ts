export type ProjectData = {
    image: string
    title: string
    client: string
    date: string
    description: string
    challenge: string
    solution: string
    results: string
    tags: string[]
}
