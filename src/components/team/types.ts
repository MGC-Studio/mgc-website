export type TeamMemberData = {
    name: string
    role: string
    image: string
    bio: string
    social: Social
}

export type Social = {
    linkedin: string
    email: string
}

export type Values = {
    icon: string,
    title: string,
    description: string
}